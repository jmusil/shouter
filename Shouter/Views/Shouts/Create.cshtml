@model Shouter.Models.Shout

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Shout</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.User.UserEmail)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.User.UserEmail)
            @Html.ValidationMessageFor(model => model.User.UserEmail)
        </div>
        
        <div class="editor-label">
            @Html.LabelFor(model => model.ShoutText)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ShoutText)
            @Html.ValidationMessageFor(model => model.ShoutText)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ShoutCreationTime)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ShoutCreationTime)
            @Html.ValidationMessageFor(model => model.ShoutCreationTime)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
